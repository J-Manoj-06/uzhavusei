import "dart:convert"; import "dart:io"; void main() { try { final bytes = File("assets/a-detailed-version.json").readAsBytesSync(); final content = utf8.decode(bytes, allowMalformed: true); final map = jsonDecode(content); print("Type: ${map.runtimeType}"); if (map is Map) { print("Cities map size: ${map.length}"); } else if (map is List) { print("Cities list size: ${map.length}"); } } catch(e) { print("Error: $e"); } }
